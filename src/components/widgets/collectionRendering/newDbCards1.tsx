import React from "react";
import { DBWidgetBase } from "../widgetTypes";

export interface DBCardsProps extends DBWidgetBase {}

export const DBCards2: React.FC<DBCardsProps> = ({
  path,
  filter,
  select,
  nonUser,
  log,
}) => {
  return <></>;
};
